package com.group5.core.util;

import com.badlogic.gdx.Input;

/**
 * Keymap for the game.
 * <p>
 * Keys are set to reasonable defaults
 */
public final class KeyMap {

    // TODO: Make this dynamically configurable

    /**
     * The key that is used for moving to the left.
     */
    public static final int LEFT = Input.Keys.LEFT;
    /**
     * The key that is used for moving to the left.
     */
    public static final int RIGHT = Input.Keys.RIGHT;

    /**
     * Hides the constructor.
     */
    private KeyMap() {
    }
}
